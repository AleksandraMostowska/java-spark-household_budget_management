package mostowska.aleksandra.api.security;

/**
 * AuthorizationRole is an enumeration that represents the various
 * authorization roles available within the application.
 */
public enum AuthorizationRole {
    USER,
    ADMIN,
    IS_AUTH
}
